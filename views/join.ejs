<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Members Only</title>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css" />
  </head>
  <body>
    <header style="display: flex">
      <h1>Members Only</h1>
      <nav>
        <% if (currentUser) {%>
        <a href="/log-out"> <button>Log Out</button></a>
        <% } else { %>
        <a href="/sign-up"><button>Sign Up</button></a>
        <a href="/log-in"><button>Log In</button></a>
        <%}%>
      </nav>
    </header>
    <main>
      <section><h3>Become a member or get admin privileges</h3></section>
      <form action="/join" method="POST">
        <label for="secret_key">Secret key</label>
        <input
          type="password"
          name="secret_key"
          id="secret_key"
          placeholder="Insert secret key"
          required
        />
        <button>Change member status</button>
      </form>
    </main>
  </body>
</html>
